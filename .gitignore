# This file exists within 'human-friendly_pedantic-timedelta' aka 'pedantic_timedelta':
#
#   https://github.com/hotoffthehamster/human-friendly_pedantic-timedelta

# Packaging — `make dist`.
build/
dist/
*.egg-info/
.eggs/

# Python byte code.
*.py[cod]
__pycache__/

# Testing/CI tasks.
.pytest_cache/
.tox

# Coverage — `make coverage`.
.coverage
# Coverage — `make coverage-html`.
htmlcov/
# Coverage — `codecov` https://codecov.io.
coverage.xml

# User's personal .ignore (e.g., .ignore.example).
.ignore

# Optional private DEV Makefile.
Makefile.local

# Sphinx Docs — `make docs`
docs/_build/
# `make docs` generates 2 files for the API reference, but one is just
# an index file (modules.rst), which we don't use; and the other is
# the auto-generated API reference. But we override the latter, as
# pedantic-timedelta.rst, so we can add additional directive options.
docs/modules.rst
docs/pedantic_timedelta.rst

# Test output for Vim quickfix — `make test-local`.
.make.out

# Translations files.
*.mo

